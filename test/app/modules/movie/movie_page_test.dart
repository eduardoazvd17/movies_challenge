import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:the_movie_challenge/app/modules/movie/pages/movie_page.dart';

void main() {
  testWidgets('MoviePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MoviePage(title: 'Movie')));
    //  final titleFinder = find.text('Movie');
    //  expect(titleFinder, findsOneWidget);
  });
}
