import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:the_movie_challenge/app/modules/movie/stores/movie_controller.dart';
import 'package:the_movie_challenge/app/modules/movie/movie_module.dart';

void main() {
  initModule(MovieModule());
  // MovieController movie;
  //
  setUp(() {
    //     movie = MovieModule.to.get<MovieController>();
  });

  group('MovieController Test', () {
    //   test("First Test", () {
    //     expect(movie, isInstanceOf<MovieController>());
    //   });

    //   test("Set Value", () {
    //     expect(movie.value, equals(0));
    //     movie.increment();
    //     expect(movie.value, equals(1));
    //   });
  });
}
